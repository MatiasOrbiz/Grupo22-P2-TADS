package Tads_Tests;

import static org.junit.Assert.*;

import org.junit.Test;
import uy.edu.um.prog2.adt.LinkedList.MiLinkedList;
import uy.edu.um.prog2.adt.Queue.EmptyQueueException;
import uy.edu.um.prog2.adt.Queue.MyQueue;

import java.util.Optional;

public class QueueImplTest {
/*
    @Test
	public void testFlujoCompleto() {
		MyQueue<Integer> queue = new Mi<Integer>() {
        };
		
		queue.enqueue(21);
		queue.enqueue(34);
		queue.enqueue(3);

        assertTrue(queue.contains(21));
        assertEquals(3, queue.size());

		try {

			assertEquals(Optional.of(21), queue.dequeue());

		} catch (EmptyQueueException e) {

			fail(e.getMessage());

		}

        assertFalse(queue.contains(21));

		assertEquals(2, queue.size());
		
		assertTrue(queue.contains(34));
		
		assertFalse(queue.contains(18));

        try {

            assertEquals(Optional.of(34), queue.dequeue());

        } catch (EmptyQueueException e) {

            fail(e.getMessage());

        }

        try {

            assertEquals(Optional.of(3), queue.dequeue());

        } catch (EmptyQueueException e) {

            fail(e.getMessage());

        }

        try {

            queue.dequeue();

            fail("Se espera excepcion");
        } catch (EmptyQueueException e) {

            assertTrue(true);

        }
	}
*/
}
